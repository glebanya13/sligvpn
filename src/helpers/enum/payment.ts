export enum PaymentProvider {
  STARS = "stars",
  YOOKASSA = "yookassa",
  CRYPTO = "crypto",
  BALANCE = "balance",
}

export enum PaymentStatus {
  PENDING = "pending",
  COMPLETED = "completed",
  FAILED = "failed",
  CANCELLED = "cancelled",
  EXPIRED = "expired",
}
