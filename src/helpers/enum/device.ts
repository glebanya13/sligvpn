export enum DeviceType {
  IOS = "ios",
  ANDROID = "android",
  DESKTOP = "desktop",
}

export enum OsType {
  IOS = "ios",
  ANDROID = "android",
  WINDOWS = "windows",
  MACOS = "macos",
  LINUX = "linux",
}
