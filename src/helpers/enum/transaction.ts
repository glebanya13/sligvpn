export enum TransactionType {
  PAYMENT = "payment",
  REFERRAL = "referral",
  BONUS = "bonus",
  WITHDRAWAL = "withdrawal",
}

export enum Currency {
  RUB = "RUB",
  XTR = "XTR",
  USDT = "USDT",
  BTC = "BTC",
  ETH = "ETH",
  STARS = "stars",
}

export enum BalanceType {
  REAL = "real",
  BONUS = "bonus",
}
